VITE_API_URL=http://localhost:3001
# VITE_ZATCA_ENABLED=false
# Optional build override. Prefer Settings → ZATCA / Company Details toggle (company.zatcaEnabled).
# Phase 1 TLV QR on settled receipts; Phase 2 reporting is enabled in Company settings (API).
# Never blocks settle.
# Developer portal uses API header x-dev-secret (default mesa-dev-portal)
